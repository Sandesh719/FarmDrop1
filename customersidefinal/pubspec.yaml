name: sihtest4  # Choose a suitable name for your project
description: "A new Flutter project."  # Modify as needed

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  razorpay_flutter: ^1.3.7 # Added Razorpay dependency
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
